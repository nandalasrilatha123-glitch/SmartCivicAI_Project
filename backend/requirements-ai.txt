# ==========================================================
# OPTIONAL heavy AI/ML/CV dependencies for SmartCivicAI
# Install only after confirming Python 3.14 wheel availability
# (see note at top of requirements.txt).
#
#     pip install -r requirements-ai.txt
#
# The app runs in full DEMO mode (rule-based AI, no CV) without
# ANY of these. Nothing here is imported at module load time by
# app/main.py — all imports are lazy inside the service classes
# that need them, guarded by try/except ImportError, so a missing
# package degrades to demo mode instead of crashing the server.
# ==========================================================

# ---------- Multi-agent orchestration ----------
langgraph==0.2.62
langchain-core==0.3.28

# ---------- LLM client (used by agents when AI_PROVIDER=anthropic|openai) ----------
anthropic==0.42.0

# ---------- Predictive analytics ----------
scikit-learn==1.6.0
xgboost==2.1.3
pandas==2.2.3
numpy==2.2.1
joblib==1.4.2

# ---------- Computer vision ----------
ultralytics==8.3.55   # YOLOv8
opencv-python-headless==4.10.0.84
