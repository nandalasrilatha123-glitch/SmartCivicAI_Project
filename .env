# ==========================================================
# Variables for docker-compose.yml (docker compose auto-reads a .env file
# in this directory). Copy to .env and adjust as needed — this is separate
# from backend/.env, which is only used for non-Docker local development.
# ==========================================================

JWT_SECRET_KEY=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

AI_PROVIDER=demo
ANTHROPIC_API_KEY=

CV_PROVIDER=demo

PREDICTION_PROVIDER=demo

EMAIL_ENABLED=false
APP_EMAIL=smartcivicai.project@gmail.com
ADMIN_EMAIL=nandalasrilatha123@gmail.com
EMAIL_PASSWORD=

GOOGLE_MAPS_API_KEY=

# Only set true for the very first `docker compose up` — see docs/docker-setup.md
SEED_ON_START=false
